import { HorariopipePipe } from './horariopipe.pipe';

describe('HorariopipePipe', () => {
  it('create an instance', () => {
    const pipe = new HorariopipePipe();
    expect(pipe).toBeTruthy();
  });
});
